import 'package:ipark/navbar/settings/profile/profilepage.dart';
import 'package:ipark/navbar/settings/settingsMenu.dart';

import 'package:flutter/material.dart';

class Settings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        //
        body: ProfilePage() //Profile
        );
  }
}
